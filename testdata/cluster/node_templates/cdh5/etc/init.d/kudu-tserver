#!/bin/bash

DIR=$(dirname $0)

. "$DIR/common"

function start {
  do_start "$KUDU_HOME"/sbin-release/kudu-tserver \
      -flagfile "$NODE_DIR"/etc/kudu/tserver.conf \
      -vmodule IBelongToTheMiniCluster
}

$1
